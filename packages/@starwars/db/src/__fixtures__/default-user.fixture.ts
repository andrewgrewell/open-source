import { IUser } from '../lib/types';

export const defaultUserFixture = {
  displayName: 'Default User',
  email: 'defaultuser@email.com',
  id: 'default-user-id',
  orgId: 'default-org-id',
} as IUser;
