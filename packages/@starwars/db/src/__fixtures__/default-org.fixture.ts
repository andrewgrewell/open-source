import { IOrg } from '../lib/types';

export const defaultOrgFixture = {
  code: 'default-org',
  id: 'default-org-id',
  name: 'Default Org',
  ownerId: 'default-user-id',
  pk: 'org#default-org-id',
} as IOrg;
