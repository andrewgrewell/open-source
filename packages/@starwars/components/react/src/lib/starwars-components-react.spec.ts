import { starwarsComponentsReact } from './starwars-components-react';

describe('starwarsComponentsReact', () => {
  it('should work', () => {
    expect(starwarsComponentsReact()).toEqual('starwars-components-react');
  });
});
